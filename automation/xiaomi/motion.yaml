- id: motion_detected_kitchen_light
  alias: Turn on light in Kitchen when motion detected
  trigger:
    - platform: state
      entity_id: binary_sensor.motion_sensor_158d00012965b6
      from: 'off'
      to: 'on'
  condition:
    - condition: state
      entity_id: sun.sun
      state: 'below_horizon'
    - condition: state
      entity_id: input_boolean.sleep_mode
      state: 'off'
  action:
    - service: light.turn_on
      entity_id: light.kitchen

- id: motion_not_detected_kitchen_light
  alias: Turn off light in Kitchen when motion detected
  trigger:
    - platform: state
      entity_id: binary_sensor.motion_sensor_158d00012965b6
      to: 'off'
      for:
        minutes: 1
  action:
    - service: light.turn_off
      entity_id: light.kitchen
      
- id: motion_detected_bedroom_light
  alias: Turn on light in Bedroom when motion detected
  trigger:
    - platform: state
      entity_id: binary_sensor.motion_sensor_158d0000f954cf
      from: 'off'
      to: 'on'
  condition:
    - condition: state
      entity_id: sun.sun
      state: 'below_horizon'
    - condition: state
      entity_id: input_boolean.sleep_mode
      state: 'off'
  action:
    - service: light.turn_on
      entity_id: light.bedroom

- id: motion_not_detected_bedroom_light
  alias: Turn off light in Bedroom when motion detected
  trigger:
    - platform: state
      entity_id: binary_sensor.motion_sensor_158d0000f954cf
      to: 'off'
      for:
        minutes: 5
  action:
    - service: light.turn_off
      entity_id: light.bedroom      
      
  