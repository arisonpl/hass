# - platform: history_stats
  # name: switch_single_click
  # entity_id: binary_sensor.switch_158d0000fd2037
  # state: 'on'
  # type: count
  # start: {{ as_timestamp(now()) }}
  # end: {{ as_timestamp(now()) + 60 }}  